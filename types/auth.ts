export type LoginCredentials = {
  email: string;
  password: string;
};

export type RegisterCredentials = {
  name: string;
  age: number;
  email: string;
  password: string;
  confirmPassword: string;
};
